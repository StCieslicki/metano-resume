---
import { languages } from "../../../data/data";
import Block from "./Block.astro";
import Bar from "./Bar.astro";

const data = languages;
---

<Block title="Languages">
    {
        data.map((item) => (
                <Bar title={item.name} value={item.weight} />
        ))
    }
</Block>
